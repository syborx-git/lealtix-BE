╔═════════════════════════════════════════════════════════════════════════════╗
║                                                                             ║
║         ✅ QUERY CROSS-SELL ACTUALIZADO - product_cross_selling             ║
║                                                                             ║
╚═════════════════════════════════════════════════════════════════════════════╝


🔄 CAMBIO REALIZADO:
═══════════════════════════════════════════════════════════════════════════════

MÉTODO: getCrossSellProducts()
UBICACIÓN: WaiterDashboardRepository.java

CAMBIO DE LÓGICA:
  Antes: Obtener productos trending basados en órdenes (últimos 30 días)
  Ahora: Obtener productos desde tabla product_cross_selling


═══════════════════════════════════════════════════════════════════════════════

❌ QUERY ANTERIOR:
═══════════════════════════════════════════════════════════════════════════════

SELECT tmp.id, tmp.nombre, tmp.precio, tmp.img_url, 
       COALESCE(tmc.nombre, 'Sin categoría') as category, 
       COALESCE(COUNT(coi.id), 0) as timesOrdered 
FROM tenant_menu_product tmp 
LEFT JOIN tenant_menu_category tmc ON tmp.category_id = tmc.id 
LEFT JOIN client_order_item coi ON tmp.id = coi.product_id 
LEFT JOIN client_order co ON coi.order_id = co.id AND co.tenant_id = :tenantId 
WHERE tmp.is_active = true 
AND (co.fecha >= (NOW() - INTERVAL '30 days') OR co.id IS NULL) 
GROUP BY tmp.id, tmp.nombre, tmp.precio, tmp.img_url, tmc.nombre 
ORDER BY timesOrdered DESC, tmp.nombre ASC 
LIMIT :limit

CAMPOS RETORNADOS: 6
  [0] = id
  [1] = nombre
  [2] = precio
  [3] = img_url
  [4] = category
  [5] = timesOrdered


═══════════════════════════════════════════════════════════════════════════════

✅ QUERY NUEVO:
═══════════════════════════════════════════════════════════════════════════════

SELECT p.id, p.nombre, p.precio, p.img_url 
FROM tenant_menu_product p 
INNER JOIN product_cross_selling cs ON p.id = cs.suggested_product_id 
INNER JOIN tenant_menu_category c ON p.category_id = c.id 
WHERE cs.tenant_id = c.tenant_id 
AND cs.tenant_id = :tenantId 
GROUP BY p.id, p.nombre, p.precio, p.img_url 
LIMIT :limit

CAMPOS RETORNADOS: 4
  [0] = id
  [1] = nombre
  [2] = precio
  [3] = img_url


═══════════════════════════════════════════════════════════════════════════════

📝 CAMBIOS EN SERVICE IMPLEMENTATION:
═══════════════════════════════════════════════════════════════════════════════

Archivo: WaiterDashboardServiceImpl.java
Método: getCrossSellSuggestions()

ACTUALIZACIÓN EN EL MAPEO:
  Antes intentaba mapear 6 campos (incluyendo category y timesOrdered)
  Ahora mapea 4 campos (solo id, nombre, precio, img_url)

MAPEO ACTUALIZADO:
  row[0] → id (Long)
  row[1] → name (String) ← nombre
  row[2] → price (BigDecimal) ← precio
  row[3] → imageUrl (String) ← img_url
  row[4] → category ("") ← VACÍO (no viene del query)
  row[5] → suggestedFor ("MESERO") ← VALOR FIJO
  row[6] → stock (0L) ← VALOR POR DEFECTO


═══════════════════════════════════════════════════════════════════════════════

🎯 RESPUESTA DEL ENDPOINT:
═══════════════════════════════════════════════════════════════════════════════

GET /api/dashboard/waiter/cross-sell?tenantId=24&limit=5

RESPUESTA (200 OK):
[
  {
    "id": 1,
    "name": "Licor Premium",
    "price": 25.99,
    "imageUrl": "https://cdn.../licor.jpg",
    "category": "",              ← Vacío (no viene en query)
    "suggestedFor": "MESERO",
    "stock": 0                   ← Por defecto
  },
  {
    "id": 5,
    "name": "Bebida Especial",
    "price": 15.50,
    "imageUrl": "https://cdn.../bebida.jpg",
    "category": "",
    "suggestedFor": "MESERO",
    "stock": 0
  },
  ...
]


═══════════════════════════════════════════════════════════════════════════════

📊 VENTAJAS DEL NUEVO QUERY:
═══════════════════════════════════════════════════════════════════════════════

✓ Usa tabla product_cross_selling (datos predefinidos)
✓ Más rápido: menos JOINs
✓ Más eficiente: sin cálculos de trending
✓ Datos controlados: administrador define cross-sells
✓ Menos complejidad SQL


═══════════════════════════════════════════════════════════════════════════════

🚀 PRUEBA EL CAMBIO:
═══════════════════════════════════════════════════════════════════════════════

1. Compilar:
   mvn clean install

2. Ejecutar:
   mvn spring-boot:run

3. Probar endpoint:
   GET http://localhost:8080/api/dashboard/waiter/cross-sell?tenantId=24&limit=5

   Headers:
     Authorization: Bearer {token}

4. Verificar respuesta:
   - Status: 200 OK
   - Array de productos
   - 4 campos principales (id, name, price, imageUrl)


═══════════════════════════════════════════════════════════════════════════════

⚠️ NOTA IMPORTANTE:
═══════════════════════════════════════════════════════════════════════════════

El query ahora utiliza hardcoded tenantId = :tenantId tanto en la condición WHERE.
Asegúrate de que:
  1. La tabla product_cross_selling existe en la BD
  2. Contiene datos para tenant_id = 24
  3. El campo suggested_product_id apunta a tenant_menu_product.id


═══════════════════════════════════════════════════════════════════════════════

✨ CAMBIOS COMPLETOS:
═══════════════════════════════════════════════════════════════════════════════

✓ WaiterDashboardRepository.java - Query actualizado
✓ WaiterDashboardServiceImpl.java - Mapeo actualizado
✓ DTOs sin cambios (mantienen los 7 campos posibles)
✓ Controller sin cambios

═══════════════════════════════════════════════════════════════════════════════
